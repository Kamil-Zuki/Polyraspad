# Шаблон Документации Микросервиса STEOS

Укороченные копии `(Done) Authorization Service/` — **та же структура папок и файлов**, в каждом документе только начало (образец формы).

## Эталон

`(Done) Authorization Service/` — полная документация.

## Staging

`99 - Staging — Разрывы согласованности (DO NOT DELETE)/` — не удалять; фиксация противоречий между `03`, `01`, `02`, `04`.
