# Название фичи (Feature Name)

**Статус:** Draft / In Progress / Implemented
**Связанный бекенд:** [Название сервисов и эндпоинты, например, Vocabulary Service (SR-VOC-01)]

## 1. UX-сценарий (User Journey)
*Краткое описание того, как пользователь взаимодействует с интерфейсом шаг за шагом.*
* **Шаг 1:** ...
* **Шаг 2:** ...

## 2. Маршрутизация и Страницы (Routing)
*Список маршрутов в `app/`, которые относятся к данной фиче.*
* `app/.../page.tsx` (Server/Client) — краткое описание ответственности.
* `app/.../layout.tsx`

## 3. Дерево компонентов (Component Architecture)
*Иерархия ключевых React-компонентов (разделение на Server/Client).*
* `<ComponentA>` (Server) — ответственность.
  * `<ComponentB>` (Client) — ответственность.

## 4. Интеграция с API (Data Fetching & BFF)
*Как фронтенд общается с бекендом в рамках этой фичи.*
* **Чтение (Queries):** Какие эндпоинты вызываются (например, через `useQuery` или `fetch` на сервере).
* **Мутации (Mutations):** Какие действия изменяют состояние (например, через `useMutation`).
* **BFF (Backend-For-Frontend):** Если используются серверные API-роуты (`app/api/...`), описать зачем (например, проксирование ключей AI).

## 5. Управление состоянием (State Management)
*Где и как хранятся данные на клиенте.*
* Локальное состояние (React `useState`, `useReducer`).
* Глобальный контекст (React Context, если используется).
* Кэш React Query (ключи инвалидации).

## 6. Стратегия тестирования фронтенда (UI Testing)
*Подход к тестированию данного UI.*
* **Компонентные тесты (Vitest + RTL):** Что именно нужно протестировать (например, рендер, клики).
* **Мокирование (MSW):** Какие эндпоинты нужно замокать для проверки успешных и ошибочных сценариев.
