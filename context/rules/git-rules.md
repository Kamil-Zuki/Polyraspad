# Git Rules

- Do not revert unrelated user changes.
- Avoid destructive commands unless the user explicitly asks for them.
- Keep commits focused when the user asks for commits.
- Do not mix documentation reorganization with feature implementation unless requested.
