# Implementation Prompt

Implement the requested change using the active plan and project rules.

Read:

- `AGENTS.md`
- `context/README.md`
- relevant `context/rules/*`
- relevant `context/plans/active/*`

Then inspect the code paths, make focused edits, and run the smallest useful verification.
