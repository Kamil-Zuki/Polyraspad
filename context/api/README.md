# API Notes

Use this folder for agent-facing API notes and contract sketches.

Official API documentation belongs in `Docs/`.

When API changes become stable, update the relevant file in `Docs/` and keep a short implementation note here if agents still need it.
