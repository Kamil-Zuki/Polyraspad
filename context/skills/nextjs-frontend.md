# Skill: Next.js Frontend Work

Use this skill for `polyraspad-frontend/`.

## First Reads

- relevant page/component;
- existing hooks in `src/lib/react-query/`;
- API clients in `src/lib/api/`;
- `context/rules/frontend-rules.md`.

## Rules

- Use existing component and hook patterns.
- Keep app screens task-first.
- Verify important UI changes with tests or a local build.
- Avoid introducing new global state unless the existing context/query patterns are insufficient.
