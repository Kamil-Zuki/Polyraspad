---
description: "Avoid redundant explicit mapping (complements dotnet-standards)"
globs: "**/*.cs"
alwaysApply: false
---

# DTO Mapping

Avoid explicit mapping for fields with the exact same names — rely on auto-mapping. Place full DTO mapping configurations in the `AutoMapProfiles/` directory (see `01-arch-dotnet-standards.mdc`).
